<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.lab">

    <application
        android:name=".view.ParkingApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lab">
        <activity android:name=".view.activities.ConfirmationActivity"/>
        <activity android:name=".view.activities.ParkingActivity" />
        <activity
            android:name=".view.activities.ReservationActivity"
            android:label="@string/title_activity_reservation"
            android:theme="@style/Theme.Lab.NoActionBar" />
        <activity android:name=".view.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activities.CitiesActivity" />
        <activity android:name=".view.activities.RegisterActivty" />
    </application>

</manifest>